<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ai.study.mapper.WordMapper">
    <select id="selectByBookId" resultType="com.ai.study.domain.Word">
        SELECT id, book_id AS bookId, word, phonetic, meaning, sort_order AS sortOrder
        FROM word WHERE book_id = #{bookId}
        ORDER BY sort_order
    </select>

    <select id="selectRandomWords" resultType="com.ai.study.domain.Word">
        SELECT id, book_id AS bookId, word, phonetic, meaning, sort_order AS sortOrder
        FROM word WHERE book_id = #{bookId}
        ORDER BY RAND()
        LIMIT #{limit}
    </select>

    <select id="selectById" resultType="com.ai.study.domain.Word">
        SELECT id, book_id AS bookId, word, phonetic, meaning, sort_order AS sortOrder
        FROM word WHERE id = #{id}
    </select>

    <select id="selectWordsByIds" resultType="com.ai.study.domain.Word">
        SELECT id, book_id AS bookId, word, phonetic, meaning, sort_order AS sortOrder
        FROM word WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <select id="selectRandomWordsExcludeMastered" resultType="com.ai.study.domain.Word">
        SELECT id, book_id AS bookId, word, phonetic, meaning, sort_order AS sortOrder
        FROM word 
        WHERE book_id = #{bookId}
        <if test="masteredIds != null and masteredIds.size() > 0">
            AND id NOT IN
            <foreach collection="masteredIds" item="id" open="(" separator="," close=")">
                #{id}
            </foreach>
        </if>
        ORDER BY RAND()
        LIMIT #{limit}
    </select>

    <select id="selectRandomWordsExcludeIds" resultType="com.ai.study.domain.Word">
        SELECT id, book_id AS bookId, word, phonetic, meaning, sort_order AS sortOrder
        FROM word 
        WHERE book_id = #{bookId}
        <if test="excludeIds != null and excludeIds.size() > 0">
            AND id NOT IN
            <foreach collection="excludeIds" item="id" open="(" separator="," close=")">
                #{id}
            </foreach>
        </if>
        ORDER BY RAND()
        LIMIT #{limit}
    </select>
</mapper>
